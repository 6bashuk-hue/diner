# מדריך הקמת מדפסת קופה אוטומטית — HPRT TP801

המטרה: מחשב קופה שפתוח על דף האדמין כל הזמן. כל הזמנה חדשה שמתקבלת מהאתר מודפסת אוטומטית במדפסת המטבח, ללא חלון אישור.

> **שים לב:** לפני שמתחילים — ודא שמיזגת את ה-PR האחרון של האתר (זה שמכיל את כפתור "🖨 הדפסה אוטומטית" בסרגל העליון של האדמין). אם לא רואים את הכפתור באדמין, הקוד עוד לא הגיע.

---

## פרטי המדפסת שלך

מתוך ה-self-test:

| שדה | ערך |
|-----|-----|
| Brand / Model | HPRT TP801 |
| Interface | USB + Ethernet + Serial |
| IP Address | `192.168.1.143` |
| MAC | `74:C9:0F:87:1B:83` |
| נייר | 80mm |
| מהירות | עד 300mm/s |
| חותך / Cutter | YES |
| Beeper / זמזם | YES |
| מגירה (drawer) | YES |

יש 3 דרכים לחבר אותה למחשב הקופה: **Ethernet (מומלץ)**, USB או Serial. המדריך הזה מניח Ethernet כי זה הכי יציב.

---

## שלב 1 — הגדרת המדפסת ברשת

המדפסת כבר מוגדרת ב-IP `192.168.1.143` (DHCP מכובה — ה-IP קבוע, מצוין).

**ודא שהמחשב על אותה רשת:**
1. Windows: פתח Command Prompt → הקלד `ping 192.168.1.143`
2. אם אתה רואה תגובות (`Reply from 192.168.1.143`) — הכל בסדר.
3. אם לא — בדוק שהמחשב והמדפסת מחוברים לאותו ראוטר.

---

## שלב 2 — התקנת דרייבר HPRT TP801

### Windows:
1. הורד את הדרייבר מ-https://www.hprt.com (או חפש "HPRT TP801 driver Windows" בגוגל).
2. בעת ההתקנה בחר **"Network printer"** (לא USB).
3. הזן את ה-IP `192.168.1.143` כאשר הוא מבקש.
4. שם המדפסת: `HPRT TP801` (או כל שם שתבחר).

### macOS:
1. **System Settings** → **Printers & Scanners** → **Add Printer**
2. לשונית **IP** → Address `192.168.1.143`
3. Protocol: **HP Jetdirect — Socket**
4. Use: **Generic PostScript Printer** (אם אין דרייבר ייעודי) או **HPRT TP801**

---

## שלב 3 — הגדרת המדפסת כברירת מחדל

**Windows:**
1. **Settings** → **Bluetooth & devices** → **Printers & scanners**
2. כבה את **"Let Windows manage my default printer"**
3. לחץ על HPRT TP801 → **Set as default**

**macOS:**
1. **System Settings** → **Printers & Scanners**
2. **Default printer** → בחר **HPRT TP801**

---

## שלב 4 — הגדרת גודל נייר 80mm (חשוב!)

המדפסת היא רוחב 80mm רציף (אין דפים). אם לא תגדיר את זה, הדפדפן יחתוך את הקבלה ל-A4 ויקצץ את התוכן.

**Windows:**
1. **Settings** → **Printers & Scanners** → לחץ על **HPRT TP801** → **Printer preferences**
2. לשונית **Paper/Quality** או **Layout**
3. **Paper Size** → **Custom** → רוחב `80mm`, גובה `297mm` (או הגבול המקסימלי)
4. אם יש אופציה "Receipt" או "80mm × ∞" — בחר אותה.
5. **Save**

**macOS:**
- בעת הדפסה, תפריט גדלי נייר → **Manage Custom Sizes** → צור 80mm × 297mm → שמור כ-"80mm Receipt".

---

## שלב 5 — בדיקת הדפסה ידנית

לפני שעוברים לאוטומציה — ודא שהדפסה רגילה עובדת:

1. פתח את https://your-site.netlify.app/admin.html במחשב הקופה.
2. התחבר עם הסיסמה.
3. בצע הזמנת בדיקה מהטלפון (או ממחשב אחר).
4. כשההזמנה מופיעה באדמין, לחץ על כפתור **🖨** בכרטיס.
5. אמור לפתוח חלון הדפסה. ודא ש:
   - המדפסת הנבחרת היא **HPRT TP801**
   - גודל נייר **80mm**
   - הקבלה נראית כמו שצריך בתצוגה המקדימה
6. לחץ **Print**.

אם הקבלה יוצאת חתוכה או רחוקה — תקן את גודל הנייר ב-step 4 ונסה שוב.

---

## שלב 6 — Chrome במצב kiosk-printing (הסוד!)

כעת הדבר שיהפוך את ההדפסה לאוטומטית: דגל מיוחד של Chrome שמדלג על דיאלוג ההדפסה ומדפיס מיד אל ברירת המחדל.

### יצירת קיצור דרך עם הדגל

#### Windows
1. לחץ ימני על שולחן העבודה → **New** → **Shortcut**
2. בשדה ה-location הדבק:
   ```
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --kiosk-printing --no-first-run --disable-features=PrintCompositorLPAC https://your-site.netlify.app/admin.html
   ```
   (אם Chrome מותקן ב-`Program Files (x86)`, תקן את הנתיב.)
3. שם הקיצור: **מדפסת קופה**
4. **Finish**

#### Mac (Terminal):
```bash
open -a "Google Chrome" --args --kiosk-printing https://your-site.netlify.app/admin.html
```

ניתן לשמור כ-Automator app אם תרצה אייקון.

### מה הדגל עושה?
`--kiosk-printing` גורם ל-Chrome להדפיס ישירות לברירת המחדל בכל פעם ש-`window.print()` נקרא — בלי דיאלוג, בלי אישור.

> ⚠️ **אזהרה:** הדגל מבטל את כל דיאלוגי ההדפסה ב-Chrome. השתמש בו רק על המחשב המייעוד לקופה.

---

## שלב 7 — הפעלת ההדפסה האוטומטית

1. הפעל את הקיצור שיצרת (Chrome ייפתח על האתר).
2. התחבר לאדמין.
3. למעלה בסרגל יש כפתור **🖨 הדפסה אוטומטית כבויה** (אפור).
4. לחץ עליו — הוא יהפוך לירוק: **🖨 הדפסה אוטומטית פעילה**.
5. בצע הזמנת בדיקה מהטלפון.
6. תוך 8 שניות — הקבלה אמורה לצאת מהמדפסת אוטומטית!

המצב נשמר ב-localStorage, כך שאם המחשב אותחל או הדפדפן נסגר — בכל פעם שתפתח את הקיצור הוא יהיה במצב פעיל.

---

## שלב 8 (אופציונלי) — הפעלה אוטומטית כשהמחשב נדלק

**Windows — Task Scheduler:**
1. פתח **Task Scheduler** (מתוך תפריט התחל).
2. **Create Basic Task** → שם: "Kitchen Display"
3. **Trigger** → "When I log on"
4. **Action** → "Start a program"
5. Program: `chrome.exe`
6. Arguments: `--kiosk-printing --no-first-run https://your-site.netlify.app/admin.html`
7. **Finish**

**Mac:**
1. **System Settings** → **General** → **Login Items**
2. **+** → בחר את ה-Automator app שיצרת בשלב 6.

---

## פתרון תקלות

### "הקבלה יוצאת ריקה / רק עם רעש"
- בעיית קודי בקרה. בדוק בהגדרות המדפסת ב-Windows שה-**Code Page** מתאימה לעברית. ב-Self-test הראית ש-`Default Code Page = Page 36 (Hebrew)` — מצוין, אבל במנהל ההתקן יש להבטיח את אותו דבר.

### "Chrome מבקש אישור הדפסה למרות הדגל"
- ודא שהפעלת עם הדגל (לחץ ימני → Properties → בדוק שורת Target).
- ייתכן שיש לך פרופיל אחר של Chrome ש-`--kiosk-printing` לא חל עליו. סגור את כל חלונות ה-Chrome קודם.

### "המדפסת לא מגיבה"
- `ping 192.168.1.143` — אם לא עונה, המדפסת לא ברשת.
- בדוק שכל המכשירים על אותה רשת.
- אתחל מדפסת.

### "מדפיס פעמיים את אותה הזמנה"
- שלא קרה — האדמין שומר ב-localStorage רשימת מפתחות שכבר הודפסו ולא מדפיס שוב, גם לא בריענון.

### "לפעמים הזמנה לא מודפסת"
- ודא שהמחשב לא ישן. ב-Windows: **Power Settings** → **Never sleep** למחשב הקופה.
- ודא שהאינטרנט יציב.

---

## דרכים מתקדמות (אם תרצה לשדרג בעתיד)

1. **ESC/POS ישיר ל-IP 192.168.1.143:9100** — שולח פקודות מעוצבות מושלמות (כולל הפעלת חותך, זמזם), אבל דורש שירות Node.js ברקע במחשב הקופה.
2. **PrintNode** — שירות ענן עם agent מקומי, כ-$5/חודש. מאפשר הדפסה גם מטלפון בלי צורך במחשב פעיל.
3. **Raspberry Pi מטבח-תרמי-בלבד** — Pi קטן ב-200₪ שמחובר למדפסת, מאזין ל-Firebase, מדפיס ESC/POS. אין מחשב קופה כלל.

תגיד אם תרצה שאבנה אחת מהדרכים האלה.
