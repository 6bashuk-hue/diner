{
  "name": "diner-kitchen-app",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for admin.html (kitchen display) — native USB thermal printing without root.",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "node scripts/sync-web.js",
    "android": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^7.0.2"
  }
}
