# מצב מטבח (Kitchen Mode) — תיעוד

## מה זה
תוספת **תוספתית ומבודדת** לדף ניהול ההזמנות הקיים (`admin.html`). הופכת אותו למסך מטבח 24/7
עם התראה אגרסיבית על הזמנה חדשה: צליל בלולאה, מסך מהבהב באדום, באנר ענק, ויברציה (אנדרואיד),
ומסך תמיד-דלוק (Wake Lock). מותאם ל-iPhone 13 / iOS 17+.

**כשמצב המטבח כבוי — הדף מתנהג בדיוק כמו לפני. אפס שינוי.**

## איך מפעילים
- כפתור **"🍳 הפעל מצב מטבח"** ב-topbar של דף ניהול ההזמנות.
- לחיצה ראשונה (מחווה אמיתית) פותחת אודיו + מבקשת Wake Lock + (ב-iOS) מציגה הוראות הגדרה.
- המכשיר **זוכר** את המצב: אחרי refresh/restart הוא חוזר אוטומטית למצב מטבח. (האודיו נפתח שוב
  בנגיעה הראשונה במסך אחרי טעינה — מגבלת iOS.)
- כיבוי: לחיצה נוספת על הכפתור → אישור.

## מה השתנה בקוד
**קובץ יחיד ששונה: `6 bashuk/admin.html`** (כל התוספות מסומנות `Kitchen Mode`):
1. **`<head>`** — נוספו תגי PWA לאדמין: `<link rel="manifest" href="/admin-manifest.json">`,
   `theme-color`, `apple-mobile-web-app-title`, `apple-touch-icon`. (לא נגעתי בתגים הקיימים.)
2. **Topbar** — כפתור אחד: `#kitchen-toggle-btn`.
3. **CSS** — בלוק מסומן `Kitchen Mode Feature`, כל ה-classes ב-`kitchen-` + `body.kitchen-*`.
4. **HTML** — בלוק מסומן בסוף ה-body: status-bar, באנר התראה, `<audio>`, כפתור בדיקה, מודאל הוראות iOS.
5. **JS** — `<script>` נפרד עם IIFE מבודד; כל המשתנים/פונקציות ב-`kitchen*`.
6. **Hook יחיד** בפונקציה הקיימת `fetchOrders` — 3 שורות מסומנות `Kitchen Mode hook` שקוראות
   ל-`window.kitchenOnNewOrders(count)` כשמתגלות הזמנות חדשות. **לא נוצר poller שני** —
   משתמשים בזיהוי ההזמנות החדשות שכבר קיים בדף.

**קבצים שנוספו:**
- `admin-manifest.json` — manifest נפרד לאדמין (`start_url: /admin.html`).
- `sounds/alert.wav` — צליל ההתראה.

**תלויות חדשות:** אין. **שינויים ב-Firebase / בפונקציות שרת:** אין.

## סטיות מהמפרט (ולמה)
1. **manifest:** המפרט נתן `start_url: "/admin/orders"` ו-manifest גלובלי. הדף הוא `/admin.html`
   וכבר קיים `manifest.json` של אתר הלקוחות. כדי לא לשבור את ה-PWA של הלקוחות, נוצר
   `admin-manifest.json` נפרד עם `start_url: "/admin.html"` והוא מקושר רק מ-`admin.html`.
2. **צליל:** המפרט הניח `/sounds/alert.mp3`. במקום, נוצר `sounds/alert.wav` (Safari מנגן WAV),
   עם fallback ל-WebAudio אם הקובץ נכשל. אם תעדיף MP3 — החלף את הקובץ ואת ה-`<source>`.
3. **Poller:** במקום poller שני (כמו במפרט), חובר hook לזיהוי הקיים ב-`fetchOrders` — מונע
   הכפלת קריאות ל-Firebase ומנצל את דגל `firstPoll` הקיים (לא מתריע על הזמנות ישנות).
4. **כפתור הפעלה:** שולב ב-topbar הקיים במקום כפתור צף (שהיה מכסה את ה-topbar).
5. **"זכירת מכשיר":** מצב המטבח נזכר (localStorage), אך ההתחברות נשארת כרגיל — לא נשמרת
   סיסמה במכשיר (סיכון אבטחה).

## הגדרות חובה ל-iPhone במטבח
1. **Auto-Lock = Never:** Settings → Display & Brightness → Auto-Lock → Never (קריטי!).
2. **Silent כבוי:** המתג הפיזי בצד שמאל למעלה — בלי כתום.
3. **Volume מקסימום.**
4. **חיבור לחשמל קבוע.**
5. **הוסף למסך הבית:** Safari → שיתוף ⬆️ → "Add to Home Screen" → פתח מהאייקון. (Wake Lock
   יציב יותר, בלי URL bar.)

### מה לא יעבוד ב-iPhone (לעומת אנדרואיד)
- ❌ ויברציה (אין API ב-Safari) — מפצים במסך מהבהב מהיר + באנר גדול.
- ❌ Push ברקע — כל עוד הדף פתוח בקדמת המסך, ההתראה עובדת.
- ❌ פתיחת אפליקציה אחרת תעצור את הצליל (מגבלת iOS).
### מה כן עובד מצוין
- ✅ צליל חזק בלולאה כשהדף בקדמת המסך · ✅ מסך מהבהב · ✅ באנר ענק · ✅ Wake Lock (עם Auto-Lock=Never).

## איך להסיר את הפיצ'ר לגמרי
1. ב-`admin.html` הסר את 4 הבלוקים המסומנים `Kitchen Mode` (head, topbar button, CSS, HTML, JS).
2. ב-`admin.html` הסר את 3 השורות המסומנות `Kitchen Mode hook` בתוך `fetchOrders`.
3. מחק `admin-manifest.json` ואת התיקייה `sounds/`.
הדף יחזור למצבו הקודם.

## בדיקות
ראה הרשימה ב-PR. בדיקות אי-רגרסיה + לוגיקה אומתו. **בדיקות שדורשות iPhone פיזי**
(מסך נשאר דלוק 10 דק', צליל מחדר אחר, הוספה למסך הבית) — יש להריץ על המכשיר במטבח.
