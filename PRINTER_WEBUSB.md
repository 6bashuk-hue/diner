# הדפסה אוטומטית ישירה (WebUSB) — מדפסת תרמית

## מה זה
מאפשר ל**טאבלט שליד המדפסת** להדפיס קבלות **אוטומטית, מקומית, דרך USB** — בלי דיאלוג,
בלי שירות ענן, בלי אינטרנט בשביל ההדפסה עצמה. תוספת מבודדת ל-`admin.html`.

הקבלה מרונדרת כ**תמונה** ונשלחת כ-raster (ESC/POS `GS v 0`), כך שעברית RTL יוצאת מושלמת
ולא תלויה ב-code page של המדפסת.

## לא פוגע ב-אביב קופות
ה-USB וה-LAN הם **ממשקים נפרדים** במדפסת. אביב ממשיכה להדפיס דרך ה-LAN בדיוק כמו היום;
הטאבלט מדפיס דרך ה-USB. אין שינוי בהגדרות ה-LAN. (אם המדפסת נעולה ל"ממשק יחיד" — יש
להעביר אותה ל-Multi/Auto בהגדרות המדפסת; ברירת המחדל של TP801 היא רב-ממשקית.)

## איך מפעילים (פעם אחת)
1. חבר את המדפסת לטאבלט בכבל **USB** (במצב USB-OTG; לטעינה+USB בו-זמנית — מפצל OTG עם חשמל).
2. פתח את `admin.html` ב-**Chrome אנדרואיד** (WebUSB נתמך ב-Chrome/Edge; לא ב-Safari/iOS).
3. בסרגל העליון לחץ **"🔌 חבר מדפסת USB"** → בחר את המדפסת באישור → הכפתור הופך ל**"🖨 מדפסת USB מחוברת"**.
4. הפעל **"🖨 הדפסה אוטומטית"** (הכפתור הקיים).
מעכשיו כל הזמנה חדשה מהאתר מודפסת אוטומטית דרך ה-USB. ההרשאה **נזכרת** — אחרי רענון/אתחול
הטאבלט מתחבר למדפסת לבד (בלי דיאלוג).

## מה השתנה בקוד (מינימלי)
**קובץ יחיד: `admin.html`** (הכל מסומן):
- CSS קטן לכפתור (`.escpos-connect-btn`).
- כפתור אחד ב-topbar.
- `<script>` מבודד עם המודול (כל הזיהויים `escpos*`).
- **שינוי יחיד בקוד קיים:** בלוק מסומן בתוך `printOrder()` — אם מדפסת USB מחוברת, מדפיס דרכה;
  אחרת **נופל-לאחור ל-`window.print()` הקיים בלי שינוי**. כשלא מחוברים — ההתנהגות זהה לחלוטין לקודם.

**לא שונה:** זרימת ההזמנות, תור ההדפסה, `window.print()`, הדפסת ה-kiosk הקודמת, פונקציות השרת, החוקים.

## כיוונון יחיד אם צריך
אם הקבלה יוצאת חתוכה/מוסטת — שנה בראש המודול `ESCPOS_WIDTH_DOTS` מ-`576` ל-`512`.

## בדיקות שבוצעו (סטטית)
- שלושת בלוקי הסקריפט ב-`admin.html` נטענים תקין; חבילת הבדיקות ירוקה.
- ללא מדפסת USB מחוברת — `printOrder` מתנהג בדיוק כמו קודם (אפס רגרסיה).

## ⚠️ בדיקות חובה על המכשיר (לא ניתן מכאן)
1. חבר USB → "🔌 חבר מדפסת USB" → בחר את ה-TP801.
2. לחץ 🖨 על כרטיס הזמנה → קבלה יוצאת, עברית תקינה, חיתוך תקין.
3. הזמנת בדיקה מהאתר עם "הדפסה אוטומטית" פעיל → מדפיס לבד.
4. הדפס **מ-אביב** ומהטאבלט (גם כמעט-יחד) → שתיהן יוצאות נקיות.
5. רענן את הדף → מתחבר למדפסת אוטומטית (בלי דיאלוג).
אם רוחב לא תקין → `ESCPOS_WIDTH_DOTS = 512`.

## איך להסיר
ב-`admin.html` הסר: בלוק ה-CSS המסומן, כפתור `escpos-connect-btn`, בלוק ה-`<script>` של
`WebUSB direct printing`, ואת הבלוק המסומן בתוך `printOrder`. הדף חוזר ל-`window.print()` בלבד.
